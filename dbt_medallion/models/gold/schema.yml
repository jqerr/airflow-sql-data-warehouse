version: 2

models:
  - name: dim_customers
    columns:
      - name: customer_key
        tests:
          - unique

  - name: dim_products
    columns:
      - name: product_key
        tests:
          - unique

  - name: fact_sales
    columns:
      - name: customer_key
        tests:
          - relationships:
              arguments:
                to: ref('dim_customers')
                field: customer_key
      - name: product_key
        tests:
          - relationships:
              arguments:
                to: ref('dim_products')
                field: product_key